using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SmoothieCleanUp : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //public void clean(Food item, GameObject liquid)
    //{
    //    SpriteRenderer sprite = liquid.GetComponentInChildren<SpriteRenderer>();
    //    sprite.color = new Color(item.liqColor.r, item.liqColor.g, item.liqColor.b);
    //    StartCoroutine(cleanUP());
    //}

    //public IEnumerator cleanUP()
    //{
    //        yield return new WaitForSeconds(5f);
    //        DragDrop[] trash = GameObject.FindObjectsOfType<DragDrop>();
 

    //}
}
